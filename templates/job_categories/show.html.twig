{% extends 'base.html.twig' %}

{% block title %}JobCategories{% endblock %}

{% block body %}
    <h1>JobCategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_category.id }}</td>
            </tr>
            <tr>
                <th>JobCategory</th>
                <td>{{ job_category.jobCategory }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('job_categories_index') }}">back to list</a>

    <a href="{{ path('job_categories_edit', {'id': job_category.id}) }}">edit</a>

    {{ include('job_categories/_delete_form.html.twig') }}
{% endblock %}
